'use strict';

Qt.include('Http.js');
Qt.include('Loading.js');

function getFeed(userId, page) {
//	Http.get('fan/' + userId + '/feed', {page: page}, function(response) {
//		console.debug(response);
//	});
//	loading();
}